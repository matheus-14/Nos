import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const primary = Color(0xFFE91E63); // rosa principal
  static const secondary = Color(0xFFAD1457); // vinho
  static const premium = Color(0xFFC9A14A); // dourado

  // Neutros
  static const background = Color.fromARGB(255, 156, 196, 233);
  static const surface = Colors.white;
  static const textPrimary = Color(0xFF2E2E2E);
  static const textSecondary = Color(0xFF6E6E6E);
}
